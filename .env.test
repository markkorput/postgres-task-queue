# This file should automatically be loaded by the pytest-dotenv package
# See the `env_files` configuration in the `[tool.pytest.ini_options]` section in the pyproject.toml
# These credentials match the ones in the postgres service in docker-compose.yaml
PGTQ_TEST_DB_URL=postgres://postgres:password@localhost:5432/pgtq_tests
